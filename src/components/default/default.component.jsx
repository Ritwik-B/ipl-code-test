import React from 'react';

import './default.styles.css';

const Default = () => (
  <div className="default-container">
    <h2>
      Search Field empty. Use filters to narrow down your search (default filter
      applied Player)
    </h2>
  </div>
);

export default Default;
